import Component from '@ember/component';
import { alias } from '@ember/object/computed';

export default Component.extend({
  tagName: '',
  class_names: "inline-block bg-blue-dark hover:bg-blue-darker text-white py-2 px-4 rounded-lg no-underline"
});
