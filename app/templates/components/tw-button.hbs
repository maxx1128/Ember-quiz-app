<span class={{wrapper_classes}}>
  {{#if link_to}}
    {{#link-to link_to class=class_names}}
      {{text}}
    {{/link-to}}
  {{/if}}

  {{#if action_1}}
    <button {{action action_1}} class={{class_names}}>
      {{text}}
    </button>
  {{/if}}
</span>
